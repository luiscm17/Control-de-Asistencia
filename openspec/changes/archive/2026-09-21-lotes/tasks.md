# Tasks: Lotes — Daily Lot Tracking (lotes-form → db_lots)

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 880–980 (8 files: 6 .gs + appsscript.json + tests; ~150 Config, ~150 Core, ~150 Persistence, ~120 Ingest, ~80 Menu, ~80 Errors, ~15 appsscript.json, ~200 tests) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 → PR 2 → PR 3 (feature-branch-chain on `lots-form`; each ≤350 lines) |
| Delivery strategy | auto-chain |
| Chain strategy | feature-branch-chain |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | Foundation SSOT + Errors + project manifest + menu skeleton (no DB write logic yet) | PR 1 — `lots-form` ← `lots-form-pr1-foundation` | `head -n 30 apps-script/lotes/Config.gs` + verify `Object.freeze` + `appsscript.json` `timeZone` check | N/A — no runtime harness (static scaffolding only; full harness in PR 2) | Revert `apps-script/lotes/Config.gs`, `apps-script/lotes/Errors.gs`, `apps-script/lotes/Menu.gs`, `apps-script/lotes/appsscript.json` — no DB writes to undo |
| 2 | Core save path — `guardarLotes()` upsert/delete batch with lock/audit + Persistence index + Ingest snapshot | PR 2 — `lots-form-pr1-foundation` ← `lots-form-pr2-save` | Manual GAS editor: run `setupLotes()` on COPY `19lBJHHKsusI6Eqkni-zh8us6ePoEHscEgrIPrFK-zhE` (read-only) → set `D4=21/09/2026`, fill `C8:C12`, `F4=TRUE` → check `db_lots` `A:K` rows `2026-09-21-1/3/5` + `Logger` execution | COPY of `19lBJHHKsusI6Eqkni-zh8us6ePoEHscEgrIPrFK-zhE` (gid `1098679039` `lotes-form`): checkbox `F4 FALSE→TRUE` + menu `Lotes → Guardar` → inspect `db_lots` via `SpreadsheetApp.getActive().getSheetByName("db_lots").getDataRange().getValues()` | Revert `apps-script/lotes/Core.gs`, `apps-script/lotes/Persistence.gs`, `apps-script/lotes/Ingest.gs` — `db_lots` rows created by this PR remain auditable (physical delete only by re-running save with empty `C`) |
| 3 | Rehydration + dispatch wiring + harness + verification | PR 3 — `lots-form-pr2-save` ← `lots-form-pr3-rehydrate` | Manual GAS editor dropdown: run `lotes.test.gs` harness entry `runLotesTests()` → Logger `✅/❌`; then manual: change `D4` `21/09/2026→20/09/2026` + `Lotes → Resincronizar` → assert `C8:G37` by posicion | COPY of `19lBJHHKsusI6Eqkni-zh8us6ePoEHscEgrIPrFK-zhE` (read-only) via `input#t-name-box` + `div#t-formula-bar-input` (canvas not scraped) + Sheets API `spreadsheets.values.get` for `db_lots` row counts; `Executions` tab check for no `Exceeded maximum execution time` | Revert `apps-script/lotes/Ingest.gs` (dispatch), `apps-script/lotes/Menu.gs` (`lotesOnEdit`/`onEdit`), `apps-script/lotes/tests/lotes.test.gs`; no data migration to undo (delete `lotesOnEdit` installable trigger) |

## Phase 1: Foundation / Infrastructure & Config SSOT

- [x] 1.1 Create `apps-script/lotes/appsscript.json` with `timeZone: "America/La_Paz"`, `runtimeVersion: "V8"`, `exceptionLogging: "STACKDRIVER"`, `oauthScopes: ["https://www.googleapis.com/auth/spreadsheets","https://www.googleapis.com/auth/script.scriptapp","https://www.googleapis.com/auth/script.container.ui","https://www.googleapis.com/auth/userinfo.email"]`, `dependencies: {}` — matches `apps-script/dyeing/appsscript.json` and `apps-script/yarn-settings/appsscript.json` pattern; verify `Utilities.formatDate` will default correctly.
- [x] 1.2 Create `apps-script/lotes/Config.gs` with `LOTES_CONFIG = Object.freeze({ TIMEZONE:"America/La_Paz", SHEETS:{FORM:"lotes-form", DB:"db_lots", ERRORS:"Errors"}, RANGES:{D4:"D4", F4:"F4", HEADERS:"B7:G7", FORM:"B8:G37", PAYLOAD:"C8:G37", DB_HEADERS:"A1:K1", ERRORS_HEADERS:"A1:F1"}, DB_HEADERS:["id","fecha","titulo","tipo_material","codigo_lote","color","observacion","creado","actualizado","creado_por","actualizado_por"], IDX:{ID:0, FECHA:1, TITULO:2, TIPO_MATERIAL:3, CODIGO_LOTE:4, COLOR:5, OBSERVACION:6, CREADO:7, ACTUALIZADO:8, CREADO_POR:9, ACTUALIZADO_POR:10}, LIMITS:{ROWS:30, COLS:11, ERROR_COLUMNS:6}, UI:{DEBOUNCE_MS:3000, SAVE_LABEL:"\u2611 GUARDAR", DB_HEADER_COLOR:"#e8f0fe", ERRORS_HEADER_COLOR:"#fce8e6", HEADER_PROTECTION:"Lotes frozen header — do not reorder"}, ERRORS:{EMPTY_DATE:"empty_date", LOCK_TIMEOUT:"lock_timeout", MISSING_SHEET:"missing_sheet", HEADER_MISMATCH:"header_mismatch"}})` — no literals outside this object for `D4`/`F4`/`B8:G37` in later files (spec: Config owns all ranges).
- [x] 1.3 Add `apps-script/lotes/Config.gs` helpers `lotesGetSheet_(ss,key)`, `lotesGetFormSheet_(ss)`, `lotesGetDbSheet_(ss)`, `lotesParseA1_(a1)`, `lotesParseRange_(a1Range)`, `lotesGetRange_(sheet,a1Range)` — Config-parsed `getRange` indirection so zero `getRange("D4")` literals appear outside Config; add `lotesDateKey_(value)` (`yyyy-MM-dd` via `Utilities.formatDate` `America/La_Paz`) and `lotesFechaDisplay_(value)` (`dd/MM/yyyy`), `lotesHeadersMatch_(actual,expected)`, `lotesEnsureTableSheet_(ss,sheetName,headers,formats)`, `lotesEnsureSchema(optSpreadsheet)` (idempotent: create `db_lots` with frozen `A:K` header row, `B` numberFormat `dd/MM/yyyy`, `H:I` `yyyy-MM-dd HH:mm:ss`, freeze row 1, auto-resize, warning-only protection; create `Errors` `A:F` with header `timestamp, scope, range, code, reason, user` + `#fce8e6`; ensure `lotes-form!F4` checkbox `requireCheckbox()` default `FALSE`), `lotesConfigureForm_(ss)` — mirrors `apps-script/dyeing/Config.gs` `DYEING_CONFIG` helpers.
- [x] 1.4 Create `apps-script/lotes/Errors.gs` with `lotesEditorEmail_()` (`Session.getActiveUser().getEmail()` || `Session.getEffectiveUser().getEmail()` || `"unknown"`), `lotesAuditTimestamp_(date)` (`Utilities.formatDate(date||new Date(),"America/La_Paz","yyyy-MM-dd HH:mm:ss")`), `lotesLogError_(code,reason,range,optSpreadsheet)` — best-effort append to `Errors` `A:F` (`timestamp America/La_Paz, scope=lotes, range, code, reason, user`), never throws; mirrors `apps-script/dyeing/Errors.gs` and `apps-script/yarn-settings/Errors.gs` pattern.
- [x] 1.5 Create `apps-script/lotes/Menu.gs` skeleton — `onOpen()` → `SpreadsheetApp.getUi().createMenu("Lotes").addItem("Guardar","guardarLotes").addItem("Resincronizar","rehidratarPorFecha").addToUi()` (exactly two entries, public names no trailing `_`, spec: Menu entries scenario), `onEdit(e)` delegating to `lotesOnEdit(e)` (simple trigger no-auth routing), `setupLotes()` idempotent (calls `lotesEnsureSchema()`, then `ScriptApp.newTrigger("lotesOnEdit").forSpreadsheet(SpreadsheetApp.getActive()).onEdit().create()` only if not already present — dedupe via `ScriptApp.getProjectTriggers()` filter on `getHandlerFunction()==="lotesOnEdit"`), plus `lotesNormalizeCheckboxValue_(v)` handling `TRUE/FALSE/VERDADERO/FALSO/true/false/boolean` normalization for `e.oldValue`/`e.value` checks.

## Phase 2: Core Implementation — Persistence & Save Path

- [x] 2.1 Create `apps-script/lotes/Persistence.gs` — `lotesBuildDbState_(dbSheet)` single `getValues()` on `db_lots A:K` (handle empty sheet: `getLastRow()<2` → empty `byId`/`byRow`) building `byId: {[id]:{rowNum,data}}` and `byRow: [{rowNum,data,id}]`, `lotesFindRow_(state,id)` → `entry|null`, `lotesBuildRowValues_(snapshotRow,state,posicion,fechaDisplay,fechaKey,now,editor)` returning `{values: [11] A:K, isNew:boolean, rowNum, creadoPreserved}` (preserves `creado`/`creado_por` on update), `lotesUpsertDeleteBatch_(ss,snapshot,state)` — iterates posicion 1..30, `titulo=C.trim()` non-empty → update `B:G` preserving `creado` else `appendRow` with `creado=actualizado=now`; `titulo` empty && `byId[id]` → queue `deleteRow` bottom-up (descending `rowNum` to avoid index shift); returns `{created,updated,deleted,totalN}`.
- [x] 2.2 Create `apps-script/lotes/Ingest.gs` — `lotesReadForm_(optSpreadsheet)` single batch `getDisplayValues()` on `B8:G37` via `lotesGetRange_` (payload `B8:G37` 30×6; column `B` ignored for DB — only positional index used), `D4` read via `getValue()` direct passthrough (no validation/coercion), derives `fechaKey=lotesDateKey_(fechaRaw)` and `fechaDisplay=lotesFechaDisplay_(fechaRaw)` (`Date` → formatted strings, non-Date/blank → `""`), returns `{ok, fechaRaw, fechaKey, fechaDisplay, fechaDate, rows:[{posicion:1..30, titulo:String(C).trim(), tipo_material:String(D), codigo_lote:String(E), color:String(F), observacion:String(G), rawRow}]}`; helpers `lotesIsEmptyRow_(r)`, `lotesIsDebounced_()` / `lotesMarkSaved_()` via `PropertiesService.getDocumentProperties()` key `lotes-last-save-ms` with `DEBOUNCE_MS=3000`, `lotesIsSaveCheckboxEvent_(e)` (check `e.range` is `F4` on `lotes-form`, normalize `e.oldValue`/`e.value` via `lotesNormalizeCheckboxValue_`, true only for `FALSE→TRUE`), `lotesIsDateEdit_(e)` (check `e.range` overlaps `D4` on `lotes-form` via parsed range).
- [x] 2.3 Create `apps-script/lotes/Core.gs` public `guardarLotes()` — flow: (a) `snapshot=lotesReadForm_()`, empty `fechaKey||fechaDisplay` guard → `SpreadsheetApp.getActiveSpreadsheet().toast("Seleccione una fecha en D4.")`, `lotesLogError_(EMPTY_DATE,…)`, guarded `lotesResetCheckbox_()` + return `{ok:false,reason:"empty_date"}` (spec: Empty D4 guard + Non-date pasted value); (b) `lock=LockService.getDocumentLock()`, `acquired=lotesAcquireLock_(lock)` (`tryLock(5000)` → `Utilities.sleep(1000)` → `tryLock(5000)`), if false → toast `Ocupado, reintente con Resincronizar`, log `LOCK_TIMEOUT` to `Errors`, guarded reset, return; (c) inside `try` acquire success: `state=lotesBuildDbState_(dbSheet)` single `getValues()`, snapshot `rows` → `lotesUpsertDeleteBatch_()` handling update/append + bottom-up `deleteRow()`, batch `setValues()` for updates, `SpreadsheetApp.flush()` before toast; (d) `now=lotesAuditTimestamp_()`, `editor=lotesEditorEmail_()` for audit (`creado`/`actualizado` `yyyy-MM-dd HH:mm:ss America/La_Paz`, `creado_por`/`actualizado_por` email||unknown); (e) `finally { lock.releaseLock() }` + guarded `lotesResetCheckbox_(ss)` (`set F4=FALSE` via `PropertiesService` flag `lotes-is-resetting-f4` so next `onEdit` ignores), `SpreadsheetApp.flush()`, success toast `✅ Guardado: dd/MM/yyyy — N lotes` where `N=created+updated` (deletes not counted) (spec: Success toast, Batch operation, Audit, Concurrency).
- [x] 2.4 Add `apps-script/lotes/Core.gs` helpers `lotesAcquireLock_(lock)` (5s + retry as above, returns boolean), `lotesResetCheckbox_(ss)` (sets `isResettingF4_` in `PropertiesService.getDocumentProperties()` → writes `F4=FALSE` via `lotesGetRange_` → clears flag after `flush()` + `Utilities.sleep(200)` to cover trigger propagation), `lotesAuditTimestamp_(date)` and `lotesEditorEmail_()` wrappers delegating to `Errors.gs` (keep single source or alias), plus internal `lotesFormatFechaForToast_(fechaDisplay)` if needed — ensure no `Session.getScriptTimeZone()` fallback beyond `America/La_Paz`.
- [x] 2.5 Implement delete-on-clear correctness: in `lotesUpsertDeleteBatch_` ensure bottom-up deletes are executed after all `setValues`/`appendRow` commits, and ensure all-empty `C8:C37` (every `titulo` empty) deletes all pre-existing `id` for that `fecha` while creating none and toast reports `0 lotes` (spec: All-empty save, Delete on clear, No column persisted — `B` never influences `id` beyond `posicion` index).

## Phase 3: Integration / Wiring — Rehydration & Trigger Dispatch

- [x] 3.1 Implement `apps-script/lotes/Core.gs` `rehidratarPorFecha_()` (private convention but called by public alias) — (a) read `D4` via `getValue()` passthrough; if empty/invalid `Date` → clear `C8:G37` via `lotesGetRange_(formSheet,"C8:G37").clearContent()`, re-assert `B8:B37=1..30` via `setValues([[1],[2],…,[30]])`, `SpreadsheetApp.flush()`, toast guidance, return 0; (b) otherwise derive `fechaDisplay`/`fechaKey`, single `getValues()` scan of `db_lots A:K` (no lock — read-only), build `posicion→row` map where `row[ID]==fechaKey-posicion` (canonical PK match) OR `row[FECHA]==fechaDisplay` fallback for legacy display comparison; (c) clear `C8:G37`, build 30×5 matrix `C:G` ordered by `posicion 1..30` (empty strings for missing), `setValues` on `C8:G37`, re-assert `B8:B37=1..30`, `SpreadsheetApp.flush()`, toast `↻ Sincronizado: dd/MM/yyyy — M lotes` or `— sin registros para dd/MM/yyyy, listo para cargar` (spec: Rehydrate existing date, Rehydrate date with no records, Batch operation).
- [x] 3.2 Complete `apps-script/lotes/Menu.gs` dispatch — `lotesOnEdit(e)` (installable trigger handler, public name without `_` for trigger creation) routing: if `lotesIsSaveCheckboxEvent_(e)` && !`lotesIsDebounced_()` && !`PropertiesService.getDocumentProperties().getProperty("lotes-is-resetting-f4")` → `lotesMarkSaved_()` → `guardarLotes()`; else if `lotesIsDateEdit_(e)` → `rehidratarPorFecha_()` (no lock, read-only); else no-op. Add `rehidratarPorFecha()` public alias (no underscore) → `return rehidratarPorFecha_()` for menu `Resincronizar` entry (public menu functions must not end with `_`). Ensure programmatic `F4=FALSE` reset from `lotesResetCheckbox_` sets the `lotes-is-resetting-f4` flag before write so the next `onEdit` for `F4` is ignored (spec: F4 reset does not re-trigger save, Spurious F4 edit ignored, Rehydrate after in-place D4 edit).
- [x] 3.3 Harden `apps-script/lotes/Menu.gs` `setupLotes()` header creation idempotency — compare existing `A1:K1` via `lotesHeadersMatch_`; if mismatch log `HEADER_MISMATCH` to `Errors` but do not auto-reorder (warn only, per design Migration guard). Ensure `db_lots!B` format `dd/MM/yyyy` and `H:I` format `yyyy-MM-dd HH:mm:ss` are applied via `setNumberFormat` on the relevant columns after creation.
- [x] 3.4 Isolation guard — verify `apps-script/lotes/` files contain no `import`/`require` from sibling `apps-script/*` (attendance-control, yarn-production, yarn-settings, dyeing, coneras-production, material-raw, winding, yarn-inventory) and reference only allowed built-ins `SpreadsheetApp`, `LockService`, `Session`, `Utilities`, `PropertiesService`, `ScriptApp` (spec: Isolation scenario). Add header comment to each `.gs` file documenting `INSTALL: Extensions > Apps Script > paste > Save > Reload sheet` and isolation note.

## Phase 4: Testing & Verification

- [x] 4.1 Create `apps-script/lotes/tests/lotes.test.gs` GAS Logger harness (manual, no runner) — `var lotesTestHelpers_ = {}` fixture + entry `function runLotesTests()` logging `✅/❌` — unit helpers: `lotesDateKey_` for `Date(2026,8,21)` → `2026-09-21`, blank → `""`, pasted string `"21/09/2026"` → `""` (no Script parsing); `lotesFechaDisplay_` `21/09/2026`; `lotesParseA1_`/`lotesParseRange_` for `D4`, `F4`, `B8:G37`; `lotesHeadersMatch_` true/false; `lotesReadForm_` truncation of `titulo` empty/whitespace; `lotesNormalizeCheckboxValue_` for `TRUE/FALSE/VERDADERO/FALSO/boolean/empty`; `lotesIsSaveCheckboxEvent_` `FALSE→TRUE` only (spurious `TRUE→TRUE` ignored) — constructs fake `getValues`/`getDisplayValues` arrays without touching a real sheet.
- [x] 4.2 Extend `apps-script/lotes/tests/lotes.test.gs` integration scenarios on COPY `19lBJHHKsusI6Eqkni-zh8us6ePoEHscEgrIPrFK-zhE` (read-only) — empty `D4` guard at `guardarLotes` (zero writes, toast, `F4=FALSE`, Errors row); valid `D4=21/09/2026` save creates 3 rows `2026-09-21-1/3/5` with `fecha=21/09/2026` and `B` `dd/MM/yyyy` format; re-save same `fecha` updates `tipo_material`/`codigo_lote` preserving `creado`/`creado_por` and refreshing `actualizado`/`actualizado_por` by second user `b@factory.bo`; clearing `C9` (posicion 2) deletes only `id=2026-09-21-2`; all-empty `C8:C37` deletes all `fecha` rows and toast `0 lotes`; `B8:B37` never persisted verification (scan `db_lots` for `No` values).
- [x] 4.3 Add `apps-script/lotes/tests/lotes.test.gs` rehydration scenarios — rehydrate existing `D4=20/09/2026` with `2026-09-20-1`/`2026-09-20-5` populates `C8`/`C12` and leaves others empty with `B=1..30`; missing `fecha` `22/09/2026` clears `C8:G37`; `D4` edit triggers `rehidratarPorFecha_` same as menu; `F4` reset does not re-trigger second save (guard flag); lock exhaustion (simulate double `tryLock` fail → `Ocupado, reintente con Resincronizar`, `Errors` row, `F4=FALSE`, zero partial writes).
- [x] 4.4 Manual verification on COPY of `19lBJHHKsusI6Eqkni-zh8us6ePoEHscEgrIPrFK-zhE` (never prod): paste `apps-script/lotes/` + `appsscript.json` into bound project, run `setupLotes()` once (Advanced → Go to … → Allow), reload sheet, verify `Lotes` menu shows exactly `Guardar` and `Resincronizar` (two entries, no `Ver db_lots`), exercise `F4=TRUE` → `✅ Guardado: 21/09/2026 — 5 lotes` → change `D4` to `20/09/2026` with prior data → form shows `20/09` rows → change back → original 5 rows restored exactamente (posicion fidelity), verify `Executions` tab has no `Exceeded maximum execution time`, verify `B8:B37` never changes on rehydrate except re-assert `1..30`, verify `Errors` sheet receives rows for empty-D4 and lock cases.

## Phase 5: Cleanup / Hardening

- [x] 5.1 Apply formatting and warnings — set `db_lots` header row style `DB_HEADER_COLOR #e8f0fe` bold + freeze row 1 + auto-resize `A:K`, `Errors` header `#fce8e6`; add warning-only protection `Lotes frozen header — do not reorder` on `A1:K1` (do not enforce edit block, only warn, mirroring dyeing migration); ensure `B8:G37` remains editable and `F4` remains checkbox after `setupLotes`.
- [x] 5.2 Documentation and comments — ensure each `apps-script/lotes/*.gs` file has header comment with change name `lotes`, source PRD `docs/lotes/PRD.md v0.1.0`, and `Isolation: apps-script/lotes/ only` note; add inline comments for `pk = yyyy-MM-dd-posicion`, `posicion 1 = B8`, `No never persisted`, and `F4 guard` rationale; no separate `README` needed beyond existing `docs/lotes/PRD.md`.
