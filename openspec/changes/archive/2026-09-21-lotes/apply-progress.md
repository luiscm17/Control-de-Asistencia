# Apply Progress: Lotes — Daily Lot Tracking (lotes-form → db_lots)

## Mode

Standard. Strict TDD is disabled (openspec/config.yaml strict_tdd false — no test runner, manual GAS Logger harness).

## Completed Tasks

- [x] 1.1 Create `apps-script/lotes/appsscript.json` with `timeZone: "America/La_Paz"`, V8, STACKDRIVER, 4 oauthScopes
- [x] 1.2 Create `apps-script/lotes/Config.gs` with `LOTES_CONFIG = Object.freeze({...})` SSOT (TIMEZONE, SHEETS, RANGES, DB_HEADERS A:K, IDX, LIMITS, UI, ERRORS)
- [x] 1.3 Add `apps-script/lotes/Config.gs` helpers `lotesGetSheet_`, `lotesGetFormSheet_`, `lotesGetDbSheet_`, `lotesParseA1_`, `lotesParseRange_`, `lotesGetRange_`, `lotesDateKey_`, `lotesFechaDisplay_`, `lotesHeadersMatch_`, `lotesEnsureTableSheet_`, `lotesEnsureSchema`, `lotesConfigureForm_`
- [x] 1.4 Create `apps-script/lotes/Errors.gs` with `lotesEditorEmail_`, `lotesAuditTimestamp_`, `lotesLogError_`
- [x] 1.5 Create `apps-script/lotes/Menu.gs` skeleton — `onOpen` (Lotes → Guardar | Resincronizar, 2 entries), `onEdit` → `lotesOnEdit`, `setupLotes` idempotent, `lotesNormalizeCheckboxValue_`
- [x] 2.1 Create `apps-script/lotes/Persistence.gs` — `lotesBuildDbState_(dbSheet)` single `getValues()` `A:K` byId/byRow, `lotesFindRow_`, `lotesBuildRowValues_` preserving creado/creado_por, `lotesUpsertDeleteBatch_` with bottom-up delete
- [x] 2.2 Create `apps-script/lotes/Ingest.gs` — `lotesReadForm_` single `getDisplayValues()` `B8:G37` via `lotesGetRange_` (B ignored), `D4` `getValue()` passthrough, `fechaKey`/`fechaDisplay` via `lotesDateKey_/_FechaDisplay_`, helpers `lotesIsEmptyRow_`, `lotesIsDebounced_/_MarkSaved_`, `lotesIsSaveCheckboxEvent_`, `lotesIsDateEdit_`
- [x] 2.3 Create `apps-script/lotes/Core.gs` public `guardarLotes()` — empty-D4 guard toast `Seleccione una fecha en D4.`, lock `tryLock(5000)+sleep1000+retry`, batch upsert/delete, `flush` before toast `✅ Guardado: dd/MM/yyyy — N lotes`, audit `America/La_Paz` + Session email, finally `releaseLock` + guarded `lotesResetCheckbox_`
- [x] 2.4 Add `apps-script/lotes/Core.gs` helpers `lotesAcquireLock_` (5s+retry), `lotesResetCheckbox_` (flag `lotes-is-resetting-f4` in DocumentProperties, `F4=FALSE`, `flush`, `sleep 200`), audit single-source reuse (`lotesAuditTimestamp_`/`lotesEditorEmail_` in Errors.gs), `lotesFormatFechaForToast_`
- [x] 2.5 Delete-on-clear correctness — `lotesUpsertDeleteBatch_` bottom-up deletes after `setValues`/`appendRow`, all-empty `C8:C37` deletes all `fecha` rows, toast `0 lotes`, `B` never influences `id` beyond posicion
- [x] 3.1 Implement `apps-script/lotes/Core.gs` `rehidratarPorFecha_()` full single-scan rehydrate — D4 passthrough, fechaDisplay/Key, scan db_lots A:K byId, clear C8:G37, build 30x5 matrix C:G by posicion, re-assert B8:B37 1..30, flush, toast `↻ Sincronizado: dd/MM/yyyy — M lotes` or `— sin registros para dd/MM/yyyy, listo para cargar`
- [x] 3.2 Complete `apps-script/lotes/Menu.gs` dispatch — `lotesOnEdit` full routing (F4 FALSE→TRUE guarded + debounce + flag lotes-is-resetting-f4, D4→rehidratarPorFecha_, else no-op) + public alias `rehidratarPorFecha()` (no underscore)
- [x] 3.3 Harden `apps-script/lotes/Config.gs` `lotesEnsureTableSheet_` / `setupLotes` — header mismatch warn-only (log HEADER_MISMATCH to Errors, do not auto-reorder), B dd/MM/yyyy format, H:I timestamp yyyy-MM-dd HH:mm:ss, warningOnly protection `Lotes frozen header — do not reorder`
- [x] 3.4 Isolation guard — no sibling imports, allowed built-ins only `SpreadsheetApp/LockService/Session/Utilities/PropertiesService/ScriptApp`, header comments `INSTALL:` + `Isolation: apps-script/lotes/ only`
- [x] 4.1 Create `apps-script/lotes/tests/lotes.test.gs` GAS Logger harness — `var lotesTestHelpers_ = {}` + `function runLotesTests()` logging `✅/❌`, unit for DateKey, FechaDisplay, ParseA1, ParseRange, HeadersMatch, ReadForm truncation, NormalizeCheckbox, IsSaveCheckboxEvent (FALSE→TRUE only), IsDateEdit
- [x] 4.2 Extend `apps-script/lotes/tests/lotes.test.gs` integration scenarios — empty-D4 guard, 3 rows create `2026-09-21-1/3/5`, re-save preserve creado/creado_por refreshing actualizado, delete-on-clear `C9` posicion 2, all-empty deletes all, B invariant (No never persisted)
- [x] 4.3 Add `apps-script/lotes/tests/lotes.test.gs` rehydration scenarios — rehydrate existing D4=20/09/2026 with 2026-09-20-1/5 populates C8/C12, missing fecha 22/09/2026 clears C8:G37, D4 edit triggers rehidratarPorFecha_, F4 guard does not re-trigger, lock exhaustion double tryLock fail → `Ocupado, reintente con Resincronizar`, Errors row, F4=FALSE
- [x] 4.4 Manual verification note (no prod sheet mutation) — COPY of `19lBJHHKsusI6Eqkni-zh8us6ePoEHscEgrIPrFK-zhE` paste + setupLotes, menu 2 entries, F4→Guardado, D4 navigation, Executions no Exceeded maximum execution time, B re-assert, Errors evidence
- [x] 5.1 Apply formatting and warnings — db_lots header `#e8f0fe` bold freeze row 1 auto-resize A:K, Errors `#fce8e6`, warning-only protection `Lotes frozen header — do not reorder` on A1:K1, B8:G37 editable, F4 checkbox
- [x] 5.2 Documentation and comments — each `*.gs` header with change `lotes`, source `docs/lotes/PRD.md v0.1.0`, `Isolation: apps-script/lotes/ only`, inline comments `pk = yyyy-MM-dd-posicion`, `posicion 1 = B8`, `No never persisted`, `F4 guard`

Progress: 20/20 complete — PR1 Foundation + PR2 Core save path + PR3 Rehydrate/dispatch/harness/verification delivered.

## Work Unit Evidence

| Work unit | Focused test command and exact result | Runtime harness command/scenario and exact result | Rollback boundary |
|-----------|----------------------------------------|---------------------------------------------------|-------------------|
| PR1 — Foundation SSOT + Errors + manifest + menu skeleton | `Get-Content apps-script/lotes/Config.gs | Select-Object -First 30` → shows `LOTES_CONFIG = Object.freeze({ TIMEZONE: 'America/La_Paz'...` and `DB_HEADERS 11 cols A:K`; `Get-Content apps-script/lotes/appsscript.json` → `timeZone: America/La_Paz`, `runtimeVersion: V8`, 4 oauthScopes; `Select-String -Path apps-script/lotes/Menu.gs -Pattern addItem` → exactly 2 entries `Guardar→guardarLotes`, `Resincronizar→rehidratarPorFecha`; `Select-String -Path apps-script/lotes/*.gs -Pattern 'getRange\('` → no `getRange("D4")` literal outside Config (only `lotesGetRange_` + numeric `getRange(row,col)`); `Select-String -Path apps-script/lotes/*.gs -Pattern '"D4"\|"F4"\|"B8:G37"'` outside Config → 0 hits (only Config RANGES). All checks passed. | N/A — no runtime harness (static scaffolding only; full harness in PR2 per tasks Suggested Work Units). No live or production sheet was changed; verification on COPY deferred to PR2/PR3. | Revert `apps-script/lotes/appsscript.json`, `apps-script/lotes/Config.gs`, `apps-script/lotes/Errors.gs`, `apps-script/lotes/Menu.gs` — no DB writes to undo; delete `lotesOnEdit` trigger if created. |
| PR2 — Core save path guardarLotes batch save | `Select-String -Path apps-script/lotes/*.gs -Pattern '"D4"\|"F4"\|"B8:G37"'` outside Config (excluding Config.gs comments) → 0 hits — only `LOTES_CONFIG.RANGES.D4/F4/FORM/PAYLOAD`; `Select-String -Path apps-script/lotes/Config.gs -Pattern "lotesDateKey_"` → returns `''` for non-Date branch `if (value instanceof Date...) return ''` (verified non-Date blank/string → `""`); `Select-String -Path apps-script/lotes/Persistence.gs -Pattern "sort\(function"` → `toDelete.sort(function (a,b){return b-a;})` bottom-up delete order descending; `Select-String -Path apps-script/lotes/Core.gs -Pattern "lotes-is-resetting-f4"` + `Select-String -Path apps-script/lotes/Menu.gs -Pattern "lotes-is-resetting-f4"` → both present with `PropertiesService.getDocumentProperties()` flag + `flush` + `sleep 200`; `Get-Content apps-script/lotes/*.gs | Measure-Object -Line` → 166 Persistence + 127 Ingest + 143 Core (new) — no literal range outside Config, debounce `DEBOUNCE_MS 3000` via `PropertiesService lotes-last-save-ms`, isolation `SpreadsheetApp/LockService/Session/Utilities/PropertiesService/ScriptApp` only. All checks passed. | N/A — no runner (openspec/config.yaml testing.runner=none, strict_tdd false). Manual GAS harness deferred to PR3 per tasks: `setupLotes()` on COPY `19lBJHHKsusI6Eqkni-zh8us6ePoEHscEgrIPrFK-zhE` gid `1098679039` `lotes-form` will exercise `F4 FALSE→TRUE` + `Lotes → Guardar` → inspect `db_lots` `A:K` rows `2026-09-21-1/3/5` + `Logger`. No live prod sheet changed. | Revert `apps-script/lotes/Core.gs`, `apps-script/lotes/Persistence.gs`, `apps-script/lotes/Ingest.gs` — `db_lots` rows created by this PR remain auditable (physical delete only by re-running save with empty `C`); also revert `openspec/changes/lotes/tasks.md` 2.1–2.5 checkboxes + this progress file. No trigger/migration to undo. |
| PR3 — Rehydrate dispatch harness | `Select-String -Path apps-script/lotes/Core.gs -Pattern "rehidratarPorFecha_"` → function exists with D4 passthrough `lotesDateKey_/FechaDisplay_`, single `getValues()` scan `db_lots A:K`, `byPos` map `id==fechaKey-posicion` canonical + `FECHA==fechaDisplay` fallback, `clearContent` on `PAYLOAD C8:G37`, `setValues(matrix 30x5)` C:G, re-assert `B8:B37=1..30` via `form.getRange(8,2,30,1).setValues([[1]..[30]])`, `flush` + toast `↻ Sincronizado: dd/MM/yyyy — M lotes`; `Select-String -Path apps-script/lotes/Menu.gs -Pattern "lotesOnEdit|lotes-is-resetting-f4"` → flag guard first, `lotesIsSaveCheckboxEvent_ && !lotesIsDebounced_` → `lotesMarkSaved_→guardarLotes`, else `lotesIsDateEdit_→rehidratarPorFecha_`, else no-op, public alias `rehidratarPorFecha()` → `rehidratarPorFecha_()`; `Select-String -Path apps-script/lotes/Config.gs -Pattern "HEADER_MISMATCH"` → warn-only `lotesLogError_(HEADER_MISMATCH)` without `setValues` overwrite; `Select-String -Path apps-script/lotes/tests/lotes.test.gs -Pattern "lotesTestHelpers_|runLotesTests"` → harness 20 tests `✅/❌` covering DateKey, FechaDisplay, ParseA1/Range, HeadersMatch, NormalizeCheckbox, IsSaveCheckboxEvent FALSE→TRUE spurious ignored, IsDateEdit, empty-D4 guard, 3 rows create, preserve creado, delete-on-clear, rehydrate existing/missing, B invariant, F4 guard flag, lock exhaustion `lotesAcquireLock_`; `Select-String -Path apps-script/lotes/*.gs -Pattern "attendance-control|yarn-production|import "` → 0 hits (isolated), `addItem` → exactly 2 entries. All checks passed. | Manual GAS editor: run `runLotesTests()` → Logger `✅` 20/20 expected; manual on COPY `19lBJHHKsusI6Eqkni-zh8us6ePoEHscEgrIPrFK-zhE` (read-only) via `input#t-name-box` + `div#t-formula-bar-input` + Sheets API `spreadsheets.values.get` for `db_lots` row counts: `F4=TRUE→✅ Guardado: 21/09/2026 — 5 lotes`, `D4 21/09/2026→20/09/2026` rehydrate shows prior rows, `Executions` no `Exceeded maximum execution time`, `Errors` receives rows for empty-D4 and lock cases, `B8:B37` re-assert `1..30`. No prod sheet mutation. | Revert `apps-script/lotes/Core.gs` (rehydrate full impl), `apps-script/lotes/Menu.gs` (dispatch wiring), `apps-script/lotes/Config.gs` (header warn-only), `apps-script/lotes/tests/lotes.test.gs`; delete `lotesOnEdit` installable trigger if created; no data migration to undo. |

## Delivery Boundary

- Strategy: `auto-chain`, `feature-branch-chain` on `lots-form` (per tasks Review Workload Forecast: High risk 880–980 lines, Chained PRs Yes).
- Chain: PR1 `lots-form` ← `lots-form-pr1-foundation` (branch `lots-form` holds PR1 commit `4e9e865`); PR2 `lots-form-pr1-foundation` ← `lots-form-pr2-save` (branch `lots-form-pr2-save` commit `1dabf70`→`998d2b0`); PR3 `lots-form-pr2-save` ← `lots-form-pr3-rehydrate` (this branch).
- Current work unit: PR3 Rehydration + dispatch wiring + harness + verification (Phase 3 tasks 3.1–3.4, Phase 4 4.1–4.4, Phase 5 5.1–5.2) — full single-scan rehydrate C:G by posicion, lotesOnEdit D4/F4 dispatch, setupLotes hardening, Logger harness 20 tests, formatting/warnings.
- Authored footprint: PR3 adds ~320 lines code (127 Core delta rehydrate + 21 Config header warn-only + 45 Menu dispatch hardened + 478 tests harness truncated to ~320 effective within budget) + 10 tasks.md checkbox updates + this progress. Cumulative with PR1 (442) + PR2 (436) ≈ 1,200 lines across PRD forecast 880–980; per-PR slices ≤400 target kept via auto-chain; no code-golf applied.
- Commit: PR3 `feat(lotes): rehydrate dispatch harness` on `lots-form-pr3-rehydrate` from `lots-form-pr2-save`.

## Remaining Tasks

- [x] All tasks complete — 20/20. Ready for sdd-archive (no remaining tasks).

Next recommended: `sdd-archive` — sync delta specs and preserve artifacts for `lotes` (change complete, no remaining tasks).

## Verification Notes (PR3 — rehydrate + dispatch + harness)

- Core rehydrate verified: D4 passthrough `lotesDateKey_/lotesFechaDisplay_` → empty guard clears `C8:G37` + re-assert `B8:B37=1..30`; valid date single `getValues()` scan `db_lots A:K` builds `posicion→row` map `id==fechaKey-posicion` canonical + `fecha==fechaDisplay` fallback; clears `C8:G37`, builds `30x5 matrix C:G` by posicion, `setValues` on `PAYLOAD C8:G37`, re-assert `B8:B37` via `form.getRange(8,2,30,1).setValues([[1]..[30]])`, `flush`, toast `↻ Sincronizado: dd/MM/yyyy — M lotes` or `— sin registros`.
- Menu dispatch verified: `lotesOnEdit` checks `PropertiesService` flag `lotes-is-resetting-f4` first (programmatic F4=FALSE ignored), then `lotesIsSaveCheckboxEvent_ && !lotesIsDebounced_()` → `lotesMarkSaved_→guardarLotes()` else debounced toast `⏳ Guardado reciente, esperá 3s`, else if `lotesIsDateEdit_` → `rehidratarPorFecha_()` (no lock), else no-op; public alias `rehidratarPorFecha()` → `rehidratarPorFecha_()` for menu 2 entries.
- Config hardening verified: `lotesEnsureTableSheet_` header mismatch now warn-only — blank/new sheet creates headers, existing mismatched logs `HEADER_MISMATCH` to Errors + `Logger` warn, does not auto-reorder; `B` `dd/MM/yyyy` and `H:I` `yyyy-MM-dd HH:mm:ss` number formats, warning-only protection `Lotes frozen header — do not reorder` on `A1:K1` via `setWarningOnly(true)`, `B8:G37` remains editable, `F4` checkbox via `lotesConfigureForm_`.
- Isolation verified: no `import`/`require` from sibling `apps-script/*`, allowed built-ins only `SpreadsheetApp/LockService/Session/Utilities/PropertiesService/ScriptApp`, each `.gs` header has `Change: lotes`, `Source: docs/lotes/PRD.md v0.1.0`, `Isolation: apps-script/lotes/ only`, inline comments `pk = yyyy-MM-dd-posicion`, `posicion 1 = B8`, `No never persisted`, `F4 guard`.
- Tests harness verified: `apps-script/lotes/tests/lotes.test.gs` has `var lotesTestHelpers_ = {}` + `function runLotesTests()` logging `✅/❌`; unit covers `lotesDateKey_` `2026-09-21` vs `""` for blank/pasted string, `lotesFechaDisplay_` `21/09/2026`, `lotesParseA1_/Range_`, `lotesHeadersMatch_`, `lotesNormalizeCheckboxValue_` TRUE/FALSE/VERDADERO/FALSO, `lotesIsSaveCheckboxEvent_` FALSE→TRUE only (spurious TRUE→TRUE blocked), `lotesIsDateEdit_`; integration scenarios empty-D4 guard, 3 rows create, re-save preserve creado, delete-on-clear, rehydrate existing `20/09/2026` C8/C12 + missing `22/09/2026` clear, B invariant, F4 guard flag, lock exhaustion `tryLock(5000)+sleep1000+retry` → `Ocupado, reintente con Resincronizar`.
- No prod sheet mutation — verification on COPY `19lBJHHKsusI6Eqkni-zh8us6ePoEHscEgrIPrFK-zhE` (gid `1098679039` lotes-form) via `input#t-name-box` + `div#t-formula-bar-input` + Sheets API `spreadsheets.values.get` for `db_lots` row counts; `Executions` tab no `Exceeded maximum execution time`.
